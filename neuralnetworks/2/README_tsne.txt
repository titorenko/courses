TSNE visualization for embedding weights.
1. Copy the contents of this directory to the assignment2 directory.
2. Train a model
    > model = train(10);
3. Execute
    > tsne_plot(model);
4. Save the figure.
    > print -dpng tsne.png
